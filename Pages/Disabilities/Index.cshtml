@page
@model CoHO.Pages.Disabilities.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<h1 class="padding">Index</h1>

<p class="padding">
    <a asp-page="Create">Create New</a>
</p>
<table class="table padding">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Disability[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Disability[0].InActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Disability) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InActive)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.DisabilityID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.DisabilityID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.DisabilityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
