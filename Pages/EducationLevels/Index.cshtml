@page
@model CoHO.Pages.EducationLevels.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EducationLevel[0].Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EducationLevel[0].InActive)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EducationLevel) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InActive)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EducationLevelID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EducationLevelID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.EducationLevelID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
